============
Installation
============

At the command line::

    $ easy_install pyfb-customerportal

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pyfb-customerportal
    $ pip install pyfb-customerportal
